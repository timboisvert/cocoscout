<% content_for :title, "Edit #{@configuration.name}" %>

<div class="w-full mb-10">
  <%= render partial: "shared/notice", locals: { notice: notice, alert: alert } %>
  <%= render partial: "shared/top_menu", locals: {
    breadcrumbs: [
      ["Ticketing", manage_ticketing_index_path],
      ["Seating Configurations", manage_seating_configurations_path],
      [@configuration.name, manage_seating_configuration_path(@configuration)]
    ],
    text: "Edit",
    links: []
  } %>

  <div class="max-w-3xl">
    <%= render "form", configuration: @configuration %>
  </div>
</div>
