<% content_for :title, "Audition Settings" %>

<div class="w-full">
  <%= render partial: "shared/notice", locals: { notice: notice } %>
  <%= render partial: "shared/top_menu", locals: {
    breadcrumbs: [
      ["Auditions", manage_production_auditions_path(@production)],
      ["Prepare", prepare_manage_production_call_to_audition_path(@production, @call_to_audition)]
    ],
    text: "Audition Settings",
    links: []
  } %>

  <%= render partial: "manage/auditions/lifecycle_panel", locals: { current_step: :prepare } %>

  <%= form_with(model: [:manage, @production, @call_to_audition], class: "contents") do |form| %>

    <%= render partial: "shared/top_menu", locals: {
      breadcrumbs: [],
      text: "Audition Type",
      links: []
    } %>

    <div class="mt-5 mb-7">
      <p class="text-sm text-gray-500">Choosing "In-Person" auditions will enable people to sign up for an audition, while "Video Upload" auditions will allow them to submit a video during the sign-up process.</p>
      <%= form.select :audition_type, [["In-person", "in_person"], ["Video Upload", "video_upload"]], {}, class: "block shadow-sm rounded-lg border px-3 py-2 mt-2 w-64 border-gray-400" %>
    </div>

    <%= render partial: "shared/top_menu", locals: {
      breadcrumbs: [],
      text: "Sign-up Window",
      links: []
    } %>

    <p class="text-sm text-gray-500">When should the Sign-up window open and close? While it&apos;s open, members of the public will be able to sign up for an in-person audition or submit a video audition, depending on how you've set up the auditions above.</p>

    <div class="sm:flex my-5">
      <div class="w-full sm:w-1/2 sm:mr-5">
        <%= form.label :opens_at, "Open At:" %>
        <%= form.datetime_field :opens_at, class: ["block shadow-sm rounded-lg border px-3 py-2 mt-2 w-full", {"border-gray-400": @call_to_audition.errors[:opens_at].none?, "border-pink-500": @call_to_audition.errors[:opens_at].any?}] %>
      </div>
      <div class="w-full mt-5 sm:w-1/2 sm:ml-5 sm:mt-0">
        <%= form.label :closes_at, "Close At:" %>
        <%= form.datetime_field :closes_at, class: ["block shadow-sm rounded-lg border px-3 py-2 mt-2 w-full", {"border-gray-400": @call_to_audition.errors[:closes_at].none?, "border-pink-500": @call_to_audition.errors[:closes_at].any?}] %>
      </div>
    </div>

    <div class="w-full border-t border-gray-200 pt-5 my-5">
      <%= form.submit "Update", class: "sm:w-auto rounded-lg px-3.5 py-2.5 text-white bg-pink-500 hover:bg-pink-600 inline-block font-medium cursor-pointer" %>
      <%= link_to "Back", prepare_manage_production_call_to_audition_path(@production, @call_to_audition), class: "sm:w-auto rounded-lg px-3.5 py-2.5 text-pink-500 bg-gray-200 hover:bg-pink-600 hover:text-white inline-block font-medium cursor-pointer" %>
    </div>
  <% end %>

</div>
