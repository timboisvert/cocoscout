<nav class="hidden w-56 bg-white border-r border-r-2 border-gray-200 lg:flex flex-col transition-all duration-200 ease-in-out z-20 fixed left-0 top-16 h-[calc(100vh-4rem)] justify-between">
  <div class="flex-1 overflow-y-auto">

    <div class="mt-2">
      <%
        shows_active = controller_name == "shows"
        shows_classes = shows_active ? "bg-pink-500 text-white" : "text-gray-700"

        availability_active = controller_name == "availability"
        availability_classes = availability_active ? "bg-pink-500 text-white" : "text-gray-700"

        auditions_active = controller_name == "auditions"
        auditions_classes = auditions_active ? "bg-pink-500 text-white" : "text-gray-700"

        audition_requests_active = controller_name == "audition_requests"
        audition_requests_classes = audition_requests_active ? "bg-pink-500 text-white" : "text-gray-700"

        questionnaires_active = controller_name == "questionnaires"
        questionnaires_classes = questionnaires_active ? "bg-pink-500 text-white" : "text-gray-700"
      %>

      <%= link_to "My Shows & Events", my_shows_path, class: "coustard-regular flex items-center gap-3 px-4 py-2 hover:bg-pink-500 hover:text-white transition-all font-medium #{shows_classes}" %>
      <%= link_to "My Availability", my_availability_path, class: "coustard-regular flex items-center gap-3 px-4 py-2 hover:bg-pink-500 hover:text-white transition-all font-medium #{availability_classes}" %>
      <%= link_to "My Auditions", my_auditions_path, class: "coustard-regular flex items-center gap-3 px-4 py-2 hover:bg-pink-500 hover:text-white transition-all font-medium #{auditions_classes}" %>
      <%= link_to "My Sign-ups", my_audition_requests_path, class: "coustard-regular flex items-center gap-3 px-4 py-2 hover:bg-pink-500 hover:text-white transition-all font-medium #{audition_requests_classes}" %>
      <%= link_to "My Questionnaires", my_questionnaires_path, class: "coustard-regular flex items-center gap-3 px-4 py-2 hover:bg-pink-500 hover:text-white transition-all font-medium #{questionnaires_classes}" %>
    </div>

  </div>

  <!-- Bottom section -->
  <div class="flex justify-center">
    <%= image_tag "cocoscout.png", alt: "CocoScout Logo", class: "w-32" %>
  </div>

</nav>