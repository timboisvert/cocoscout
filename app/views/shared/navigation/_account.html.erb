<nav class="hidden w-64 bg-white border-r border-r-2 border-gray-200 lg:flex flex-col transition-all duration-200 ease-in-out z-50 app-top-chrome fixed left-0 top-0 h-screen justify-between">
  <%= render "shared/navigation/nav_dashboard_selector", current_dashboard: "account" %>

  <div class="flex-1 overflow-y-auto">
    <div class="mt-2">
      <%= render "shared/navigation/sidebar_nav", context: :account %>
    </div>
  </div>

  <!-- Bottom section -->
  <div class="mt-auto">
    <div class="flex justify-center">
      <%= render "shared/cocoscout_logo", size: :medium %>
    </div>
    <%= render "shared/navigation/sidebar_account_menu" %>
  </div>

</nav>
