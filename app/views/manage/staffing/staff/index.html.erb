<% content_for :title, "Staff" %>

<div class="w-full mb-10"
     data-controller="staff-modal"
     data-staff-modal-create-url-value="<%= manage_create_staffing_staff_path %>"
     data-staff-modal-update-url-template-value="<%= manage_update_staffing_staff_path(":id") %>"
     data-staff-modal-available-people-value="<%= @available_people_payload.to_json %>">
  <%= render partial: "shared/notice", locals: { notice: notice, alert: alert } %>
  <%= render partial: "shared/top_menu", locals: {
    breadcrumbs: [
      ["Staffing", manage_staffing_index_path]
    ],
    text: "Staff",
    links: [
      { text: "Add Staff", icon: "plus", path: manage_new_staffing_staff_wizard_path }
    ]
  } %>

  <%= render "manage/staffing/staff_list" %>
</div>
