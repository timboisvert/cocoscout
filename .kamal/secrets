# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# ECR password (fetched dynamically via AWS CLI)
KAMAL_REGISTRY_PASSWORD=$(aws ecr get-login-password --region us-east-2)

# App secrets from 1password
SECRETS=$(kamal secrets fetch --adapter 1password --account CSOAAT62WNAVXLO4PXXKYVREWI --from CocoRunsEverything/CocoScoutProductionPasswords RAILS_MASTER_KEY DATABASE_URL AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY MAILGUN_API_KEY SENTRY_DSN)
RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
DATABASE_URL=$(kamal secrets extract DATABASE_URL ${SECRETS})
AWS_ACCESS_KEY_ID=$(kamal secrets extract AWS_ACCESS_KEY_ID ${SECRETS})
AWS_SECRET_ACCESS_KEY=$(kamal secrets extract AWS_SECRET_ACCESS_KEY ${SECRETS})
MAILGUN_API_KEY=$(kamal secrets extract MAILGUN_API_KEY ${SECRETS})
SENTRY_DSN=$(kamal secrets extract SENTRY_DSN ${SECRETS})