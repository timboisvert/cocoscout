<% content_for :title, "Create Audition Cycle - Select Production" %>

<div class="w-full">
  <%= render partial: "shared/notice", locals: { notice: notice, alert: alert } %>
  <%= render partial: "shared/top_menu", locals: {
    breadcrumbs: [
      ["Sign-ups", manage_signups_path]
    ],
    text: "Create Audition Cycle",
    links: []
  } %>

  <div class="max-w-2xl mx-auto mt-8">
    <!-- Progress Steps -->
    <div class="mb-8">
      <div class="flex items-center justify-center">
        <div class="flex items-center">
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium bg-pink-500 text-white">
            1
          </div>
          <span class="ml-2 text-sm font-medium text-pink-600 hidden sm:inline">Production</span>
        </div>
        <div class="flex-1 h-0.5 mx-2 bg-gray-200"></div>
        <div class="flex items-center">
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium bg-gray-200 text-gray-600">
            2
          </div>
          <span class="ml-2 text-sm font-medium text-gray-500 hidden sm:inline">Format</span>
        </div>
        <div class="flex-1 h-0.5 mx-2 bg-gray-200"></div>
        <div class="flex items-center">
          <div class="w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium bg-gray-200 text-gray-600">
            ...
          </div>
          <span class="ml-2 text-sm font-medium text-gray-500 hidden sm:inline">More steps</span>
        </div>
      </div>
    </div>

    <!-- Step Content -->
    <div class="bg-white rounded-lg border border-gray-200 shadow-sm p-6 sm:p-8">
      <h2 class="text-xl font-bold text-gray-900 mb-2 coustard-regular">Which production is this audition for?</h2>
      <p class="text-gray-600 mb-6">Search for the production this audition cycle belongs to, or pick one you've used recently.</p>

      <%
        active_counts = AuditionCycle.where(production_id: @productions.map(&:id), active: true)
                                     .group(:production_id).count
        badges = @productions.index_by(&:id).transform_values do |p|
          count = active_counts[p.id].to_i
          { text: "#{count} active", highlight: count.positive? }
        end
      %>
      <%= render "shared/production_selector",
            productions: @productions,
            url: manage_signups_auditions_save_production_selection_path,
            cancel_path: manage_signups_path,
            badges: badges %>
    </div>
  </div>
</div>
