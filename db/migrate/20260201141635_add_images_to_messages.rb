class AddImagesToMessages < ActiveRecord::Migration[8.1]
  def change
    # Images are handled via ActiveStorage, no schema change needed
    # Just a placeholder migration to document the feature addition
  end
end
