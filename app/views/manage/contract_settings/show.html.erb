<% content_for :title, "Contract Settings" %>

<%= render layout: "shared/settings_layout", locals: {
  title: "Contract Settings",
  breadcrumbs: [
    ["Contracts", manage_contracts_path]
  ],
  sections: sections,
  current: @section
} do %>
  <%= render "manage/contract_settings/sections/#{@section}" %>
<% end %>
