<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "CocoScout" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Caprasimo&family=Coustard:wght@400;900&family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" rel="stylesheet">

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="inter-regular text-slate-700 bg-[#f9f9f7]">

    <% if session[:impersonate_user_id] %>
      <% impersonated = User.find_by(id: session[:impersonate_user_id]) %>
      <% if impersonated %>
        <div class="w-full bg-pink-600 text-white py-2 px-4 text-center text-sm flex items-center justify-center z-50 fixed top-0 left-0" style="min-height: 40px;">
          <span class="mr-2">Impersonating: <%= impersonated.email_address %></span>
          <%= button_to "Stop Impersonating", stop_impersonating_user_path, method: :post, class: "ml-4 px-3 py-1 rounded-lg bg-white text-pink-500 font-bold hover:bg-gray-100 cursor-pointer" %>
        </div>
      <% end %>
    <% end %>

  <div class="flex min-h-screen <%= 'mt-[40px]' if session[:impersonate_user_id] %>">

      <% if @show_manage_sidebar %>

        <%= render "shared/sidebars/manage" %>
        <main class="flex-1 px-5 mt-5 ml-56">

      <% elsif @show_my_sidebar %>

        <%= render "shared/sidebars/app" %>
        <main class="flex-1 px-5 mt-5 ml-56">

      <% else %>

        <main class="flex-1 px-5 mt-5">

      <% end %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>
